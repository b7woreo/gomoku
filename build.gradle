buildscript {
    apply from: './config.gradle'

    repositories {
        jcenter()
    }
    dependencies {
        classpath rootProject.plugin["kotlin"]
    }
}

allprojects {
    repositories {
        jcenter()
    }
}